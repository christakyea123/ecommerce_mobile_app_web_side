# Dependencies — restore with `npm install`, never commit
node_modules/

# Build output — regenerate with `npm run build`
dist/

# Build / scratch artifacts
results.txt
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local environment overrides (e.g. the live Paystack key)
.env
.env.local
